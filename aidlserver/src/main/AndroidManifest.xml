<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.locensate.firstlinecode">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.locensate.firstlinecode.MainActivity"
            android:process="com.locensate.aidlservice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name="com.locensate.firstlinecode.MessengerService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote">
        </service>
        <service
            android:name="com.locensate.firstlinecode.AIDLService"
            android:process=":aidl"
            android:enabled="true"
            android:exported="true">
        </service>
    </application>

</manifest>